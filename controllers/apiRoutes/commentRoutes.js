const router = require('express').Router();

//GET all comments by post ID

//POST new comment, requires post ID

//PUT comment - requires user authentication

//DELETE comment - requires user authentication

module.exports = router;
