const router = require('express').Router();

//GET all users

//GET user by id

//POST add new user - used for signup

//PUT edit user by username or id. requires user authentication

//DELETE user - requires user authentication

module.exports = router;