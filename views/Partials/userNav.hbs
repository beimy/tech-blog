  {{!-- <div class="wrapper">
    <header>
      <nav class="navbar navbar-light bg-light border-bottom">
        <div class="container-fluid">
            <a class="navbar-brand flex-grow-1" href="/">LOGO</a>

            <form class="d-inline-flex input-group w-auto">
                <input
                  type="search"
                  class="form-control rounded"
                  placeholder="Search"
                  id="searchBar"
                  aria-label="Search"
                  aria-describedby="search-addon"
                />
                <span class="input-group-text border-0" id="search-addon">
                  <i class="fas fa-search"></i>
                </span>
            </form>
            {{#if loggedIn}}
            <a href="/dashboard" class="m-3">My Journeys</a>
            <button type="button" id="logout-btn" href="/user-login/logout" class="btn btn-primary m-3">
                Log Out
            </button>
            {{else}}
            <a href="/user-login">Login</a>
            {{/if}}
        </div>
      </nav>
    </header> --}}

    {{!-- User Navigation Partial --}}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">RH</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <div class="navbar-nav me-auto mb-2 mb-lg-0">
            <div class="nav-item">
              <!-- HOME/MAIL LANDING PAGE -->
              <a class="nav-link active" aria-current="page" href="http://localhost:3010/">Home</a>
            </div>
            <div class="nav-item flex-grow-1">
              <!-- ABOUT -->
              <a class="nav-link" href="#">About Rabbit Holes</a>
            </div>
            <div class="justify-content-end">
              <!--Search Bar -->
              
            </div>
            {{#if loggedIn}}
            </div>
              {{> userLogout }}
              {{else}}
            </div>
              {{> userLogin }}

            {{/if}}
        </div>
      </div>
    </nav>