<div class="container-sm w-100">
    <div class="row d-flex pt-4">

        <div class="col-10" id="post-col">
            {{#each posts as |post|}}
                {{> postContent post}}
            {{/each}}
        </div>

        {{> rightMenu}}
    </div>
</div>
